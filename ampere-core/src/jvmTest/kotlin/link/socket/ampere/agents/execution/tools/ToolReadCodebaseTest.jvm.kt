package link.socket.ampere.agents.execution.tools

actual class ToolReadCodebaseTest {

    actual fun `validateParameters requires path string`() {
    }

    actual fun `execute reads file content`() {
    }

    actual fun `execute lists directory contents`() {
    }

    actual fun `execute returns error for non-existent path`() {
    }

    actual fun `execute blocks traversal outside root directory`() {
    }
}
